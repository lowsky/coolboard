{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "type": "module",
  "engines": {
    "node": "26.x"
  },
  "packageManager": "yarn@4.18.0",
  "dependencies": {
    "@clerk/testing": "2.2.30",
    "cypress": "15.21.0",
    "typescript": "7.0.2"
  },
  "scripts": {
    "cy": "cypress",
    "test": "npx -y yarn run cy run",
    "wait:valid-version": "check-deployed-version/check-deployed-version"
  }
}
