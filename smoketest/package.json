{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "engines": {
    "node": ">=12.1"
  },
  "dependencies": {
    "cypress": "9.1.1",
    "typescript": "4.5.4"
  },
  "scripts": {
    "cy": "cypress",
    "test": "yarn run cy run",
    "wait:valid-version": "yarn --frozen-lockfile ; echo install done with exit code $? ; node wait-for-deployment.mjs"
  },
  "devDependencies": {
    "got": "12.0.0"
  }
}
