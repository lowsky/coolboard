{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "dependencies": {
    "cypress": "3.3.0"
  },
  "scripts": {
    "cy": "cypress",
    "test": "npm run cy run"
  },
  "devDependencies": {}
}
