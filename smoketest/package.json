{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "engines": {
    "node": ">=12.1"
  },
  "dependencies": {
    "cypress": "8.3.1"
  },
  "scripts": {
    "cy": "cypress",
    "test": "yarn run cy run",
    "wait:valid-version": "yarn --frozen-lockfile ; echo install done with exit code $? ; node wait-for-deployment.js"
  },
  "devDependencies": {
    "got": "11.8.2"
  }
}
