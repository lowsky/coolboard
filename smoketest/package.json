{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "engines": {
    "node": ">=12.1"
  },
  "dependencies": {
    "cypress": "5.5.0"
  },
  "scripts": {
    "cy": "cypress",
    "test": "npm run cy run",
    "wait:valid-version": "npm install request request-promise ; node wait-for-deployment.js"
  },
  "devDependencies": {
    "request": "2.88.2",
    "request-promise": "4.2.6"
  }
}
