{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "engines": {
    "node": ">=12.1"
  },
  "dependencies": {
    "cypress": "10.8.0",
    "typescript": "4.8.3"
  },
  "scripts": {
    "cy": "cypress",
    "test": "yarn run cy run",
    "wait:valid-version": "yarn --immutable ; echo install done with exit code $? ; node wait-for-deployment.mjs"
  },
  "devDependencies": {
    "got": "12.4.1"
  }
}
