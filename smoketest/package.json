{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "cypress": "12.0.2"
  },
  "scripts": {
    "cy": "cypress",
    "test": "yarn run cy run",
    "wait:valid-version": "cd check-deployed-version ; npm run install_and_test"
  }
}
