{
  "name": "coolboard-smoketests",
  "version": "0.1.0",
  "description": "run some browser tests via cypress",
  "private": true,
  "dependencies": {
    "cypress": "3.4.0"
  },
  "scripts": {
    "cy": "cypress",
    "test": "npm run cy run",
    "wait:valid-version": "node wait-for-deployment.js"
  },
  "devDependencies": {
    "request": "2.88.0",
    "request-promise": "4.2.4"
  }
}
